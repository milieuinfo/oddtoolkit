import{_ as l,C as p,o as r,c as h,a2 as n,b as s,w as t,a as o,E as c,a3 as i,j as a}from"./chunks/framework.rfIdp5fP.js";const k=JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"guide/architecture.md","filePath":"guide/architecture.md"}'),g={name:"guide/architecture.md"};function u(m,e,f,y,b,C){const d=p("Mermaid");return r(),h("div",null,[e[3]||(e[3]=n(`<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><p>This page describes how ODDToolkit is put together, so you can reason about a run or debug one. For task-oriented material see <a href="./generators">Generators</a>, <a href="./adapters">Adapters</a> and <a href="./extending">Extending</a>.</p><h2 id="shape-of-the-application" tabindex="-1">Shape of the application <a class="header-anchor" href="#shape-of-the-application" aria-label="Permalink to &quot;Shape of the application&quot;">​</a></h2><p>ODDToolkit is a plain Java 21 command-line application. It reads RDF with Apache Jena, binds configuration with Jackson, and writes files. That is the whole runtime.</p><p>There is <strong>no Spring container</strong>, no component scanning and no service discovery. Every object is constructed by hand in <code>config/OddtoolkitBootstrap.java</code>. <code>@ConfigPrefix</code> and <code>@ConditionalOnConfigProperty</code> are the project&#39;s own annotations — they look like Spring&#39;s, but they are read by a few lines of reflection in <code>ConfigurationBinder</code> and <code>OddtoolkitBootstrap</code>, not by a framework. The practical consequence: if a generator or adapter is not mentioned in <code>OddtoolkitBootstrap</code>, it does not exist at runtime.</p><p>Packages:</p><table tabindex="0"><thead><tr><th>Package</th><th>Responsibility</th></tr></thead><tbody><tr><td><code>cli</code></td><td><code>GeneratorCliRunner</code> — picks and executes generators</td></tr><tr><td><code>config</code></td><td>Argument parsing, config loading and binding, the bootstrap, the generator registry</td></tr><tr><td><code>adapter</code></td><td>The 13 pipeline stages that read and enrich the model</td></tr><tr><td><code>generator</code></td><td>The 10 generators and their base classes</td></tr><tr><td><code>model</code></td><td><code>OntologyInfo</code>, <code>ConceptSchemeInfo</code> and the info objects hanging off them</td></tr><tr><td><code>util</code></td><td>Mermaid PNG export, Markdown loading and Bikeshed conversion</td></tr></tbody></table><h2 id="bootstrap-sequence" tabindex="-1">Bootstrap sequence <a class="header-anchor" href="#bootstrap-sequence" aria-label="Permalink to &quot;Bootstrap sequence&quot;">​</a></h2><p><code>OddtoolkitApplication.main</code> does two things: build a registry, then run the CLI.</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GeneratorRegistry registry </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OddtoolkitBootstrap.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bootstrap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(args);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GeneratorCliRunner</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(registry).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(args);</span></span></code></pre></div>`,10)),(r(),s(i,null,{default:t(()=>[c(d,{id:"mermaid-87",class:"mermaid",graph:"sequenceDiagram%0A%20%20autonumber%0A%20%20participant%20M%20as%20OddtoolkitApplication.main%0A%20%20participant%20B%20as%20OddtoolkitBootstrap%0A%20%20participant%20S%20as%20ConfigurationSourceResolver%0A%20%20participant%20C%20as%20ConfigurationBinder%0A%20%20participant%20G%20as%20GeneratorConfiguration%0A%20%20participant%20R%20as%20DefaultGeneratorRegistry%0A%20%20participant%20CLI%20as%20GeneratorCliRunner%0A%0A%20%20M-%3E%3EB%3A%20bootstrap(args)%0A%20%20B-%3E%3EB%3A%20CliConfiguration.fromArgs(args)%0A%20%20B-%3E%3ES%3A%20loadFromFile(configFile)%0A%20%20S--%3E%3EB%3A%20Map%26lt%3BString%2C%20Object%26gt%3B%20(empty%20if%20absent)%0A%20%20B-%3E%3EC%3A%20bind(root%2C%20OntologyConfiguration%2C%20%E2%80%A6)%0A%20%20C--%3E%3EB%3A%20typed%20properties%20POJOs%0A%20%20B-%3E%3EB%3A%20applyCliOverrides(ontology%2Fconcepts%20paths)%0A%20%20B-%3E%3EB%3A%20new%20OntologyInfo(config)%0A%20%20B-%3E%3EB%3A%20createAdapterBeans()%20%E2%86%92%20filter%20by%20enabled%0A%20%20B-%3E%3EG%3A%20one%20factory%20call%20per%20generator%0A%20%20G--%3E%3EB%3A%20generator%20%2B%20its%20selected%2C%20sorted%20adapters%0A%20%20B-%3E%3ER%3A%20register(generator.getName()%2C%20generator)%0A%20%20B--%3E%3EM%3A%20GeneratorRegistry%0A%20%20M-%3E%3ECLI%3A%20run(args)%0A%20%20CLI-%3E%3ECLI%3A%20CliConfiguration.fromArgs(args)%20again%0A%20%20CLI-%3E%3ER%3A%20get(name)%20%2F%20getAvailableGenerators()%0A%20%20CLI-%3E%3ECLI%3A%20generator.generate()%20%E2%86%92%20run()%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[o(" Loading... ",-1)])]),_:1})),e[4]||(e[4]=n('<p>Things worth knowing about that sequence:</p><ul><li><strong>The arguments are parsed twice.</strong> <code>OddtoolkitBootstrap.bootstrap</code> parses them to find <code>--config-file</code>, <code>--ontology-file</code> and <code>--concepts-file</code>; <code>GeneratorCliRunner.run</code> parses the same array again to find <code>--generator</code> and <code>--help</code>. Both use <code>CliConfiguration.fromArgs</code>.</li><li><strong>Bootstrap always runs, even for <code>--help</code>.</strong> The registry — and therefore every generator and every adapter instance — is constructed before the help check. A failure while constructing adapters shows up even when you only asked for help.</li><li><strong><code>validate()</code> is never called by the CLI.</strong> <code>GeneratorCliRunner</code> calls <code>generate()</code>, which delegates straight to <code>run()</code>. <code>BaseGenerator.validate()</code> and <code>ODCSGenerator.validate()</code> exist but are only reachable from tests or your own code.</li><li><strong>Every generator shares one <code>OntologyInfo</code> instance and one set of adapter instances.</strong> They are created once in <code>bootstrap</code> and handed to all ten generators. With <code>--generator=all</code> the runner executes the generators in alphabetical order and each one re-runs its adapter list against that same, already-populated model.</li></ul><h2 id="configuration-binding" tabindex="-1">Configuration binding <a class="header-anchor" href="#configuration-binding" aria-label="Permalink to &quot;Configuration binding&quot;">​</a></h2><p>Configuration is a two-step affair: load a file into a nested <code>Map</code>, then bind subtrees of that map onto typed POJOs.</p><p><code>ConfigurationSourceResolver.loadFromFile</code> picks a Jackson mapper from the file extension — <code>.json</code>, <code>.yml</code> or <code>.yaml</code>. Anything else, a missing file, or a parse error produces a <strong>warning and an empty map</strong>; the run then continues on code defaults. If a run behaves as if your config did not exist, check the extension and the path first.</p><p><code>ConfigurationBinder.bind</code> reads the <code>@ConfigPrefix</code> annotation on the target class, walks that dot-path through the map, and converts the resulting subtree with a Jackson <code>ObjectMapper</code> configured with <code>PropertyNamingStrategies.KEBAB_CASE</code>. That is where the <code>kebab-case</code> key convention comes from: <code>outputFile</code> in Java is <code>output-file</code> in YAML.</p><p>Two behaviours follow from the implementation:</p><ul><li>If the section is missing or empty, <code>bind</code> returns the default instance it was handed and Jackson never runs.</li><li>If the section exists, <code>bind</code> converts it into a <strong>new</strong> instance. Keys you omitted fall back to the field initialisers on the POJO, not to the object you passed in. So partial sections are safe, but the defaults that apply are the ones declared in the properties class.</li></ul><p><code>@ConfigPrefix</code> classes and their prefixes:</p><table tabindex="0"><thead><tr><th>Prefix</th><th>Class</th></tr></thead><tbody><tr><td><code>ontology</code></td><td><code>OntologyConfiguration</code></td></tr><tr><td><code>adapters.ontology-reasoner</code></td><td><code>OntologyReasonerProperties</code></td></tr><tr><td><code>adapters.ontology-extract-external</code></td><td><code>OntologyExtractExternalAdapter.ExtractExternalProperties</code></td></tr><tr><td><code>generators.class-diagram</code></td><td><code>ClassDiagramProperties</code></td></tr><tr><td><code>generators.er-diagram</code></td><td><code>ERDiagramProperties</code></td></tr><tr><td><code>generators.diagram-generator</code></td><td><code>DiagramGeneratorProperties</code></td></tr><tr><td><code>generators.schema-generator</code></td><td><code>SchemaGeneratorProperties</code></td></tr><tr><td><code>generators.sql-generator</code></td><td><code>SQLGeneratorProperties</code></td></tr><tr><td><code>generators.java-generator</code></td><td><code>JavaGeneratorProperties</code></td></tr><tr><td><code>generators.typescript-generator</code></td><td><code>TypescriptGeneratorProperties</code></td></tr><tr><td><code>generators.shacl-generator</code></td><td><code>ShaclGeneratorProperties</code></td></tr><tr><td><code>generators.data-frame-generator</code></td><td><code>DataFrameGeneratorProperties</code></td></tr><tr><td><code>generators.bikeshed-generator</code></td><td><code>BikeshedGeneratorProperties</code></td></tr><tr><td><code>generators.odcs-generator</code></td><td><code>ODCSGeneratorProperties</code></td></tr></tbody></table><p>The <code>generators</code> section is read a second time, untyped, by <code>OddtoolkitBootstrap.readGeneratorMap</code> into <code>GeneratorProperties</code>. That raw copy is what supplies the per-generator <code>adapters</code> list. It is keyed by a <strong>different</strong> name than the typed properties class for most generators:</p><table tabindex="0"><thead><tr><th>Generator</th><th><code>adapters</code> list read from</th><th>Typed settings read from</th></tr></thead><tbody><tr><td><code>class</code></td><td><code>generators.class</code></td><td>—</td></tr><tr><td><code>data-frame</code></td><td><code>generators.class</code></td><td><code>generators.data-frame-generator</code></td></tr><tr><td><code>class-diagram</code></td><td><code>generators.class-diagram</code></td><td><code>generators.class-diagram</code></td></tr><tr><td><code>er-diagram</code></td><td><code>generators.er-diagram</code></td><td><code>generators.er-diagram</code></td></tr><tr><td><code>sql</code></td><td><code>generators.sql</code></td><td><code>generators.sql-generator</code></td></tr><tr><td><code>shacl</code></td><td><code>generators.shacl</code></td><td><code>generators.shacl-generator</code></td></tr><tr><td><code>java</code></td><td><code>generators.java</code></td><td><code>generators.java-generator</code></td></tr><tr><td><code>typescript</code></td><td><code>generators.typescript</code></td><td><code>generators.typescript-generator</code></td></tr><tr><td><code>bikeshed</code></td><td><code>generators.bikeshed</code></td><td><code>generators.bikeshed-generator</code></td></tr><tr><td><code>odcs</code></td><td><code>generators.odcs</code></td><td><code>generators.odcs-generator</code></td></tr></tbody></table><p>Note the two oddities: <code>data-frame</code> picks up the adapter list configured for <code>class</code>, and the schema/code generators read their <code>adapters</code> list from the short key while their settings live under the <code>-generator</code> key. If a custom <code>adapters</code> list appears to be ignored, that mismatch is the usual cause.</p><p>See <a href="./configuration">Configuration Reference</a> for the field-by-field reference.</p><h2 id="adapter-pipeline" tabindex="-1">Adapter pipeline <a class="header-anchor" href="#adapter-pipeline" aria-label="Permalink to &quot;Adapter pipeline&quot;">​</a></h2><p>An adapter is a subclass of <code>AbstractAdapter&lt;T extends AbstractInfo&gt;</code> with one abstract method:</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> abstract</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> T </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">adapt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(T info);</span></span></code></pre></div><p><code>AbstractAdapter</code> knows the <code>Class&lt;T&gt;</code> it can handle, which drives <code>canAdapt(AbstractInfo)</code>. <code>BaseGenerator.run()</code> walks its adapter list and, for each adapter, offers it both the <code>OntologyInfo</code> and the <code>ConceptSchemeInfo</code>; an adapter runs against whichever of the two it declares. Adapters mutate the info objects in place and return them.</p><p><code>OddtoolkitBootstrap.createAdapterBeans</code> instantiates all 13 adapters into a <code>LinkedHashMap</code> keyed by name, then drops the disabled ones. Enablement is decided by <code>@ConditionalOnConfigProperty(prefix = &quot;adapters&quot;, name = &quot;&lt;name&gt;.enabled&quot;, havingValue = &quot;true&quot;, matchIfMissing = true)</code> — every adapter currently carries one, so every adapter defaults to enabled and is switched off with <code>adapters.&lt;name&gt;.enabled: false</code>. Adapters without the annotation fall back to the same <code>adapters.&lt;name&gt;.enabled</code> lookup.</p><h3 id="ordering" tabindex="-1">Ordering <a class="header-anchor" href="#ordering" aria-label="Permalink to &quot;Ordering&quot;">​</a></h3><p><code>GeneratorConfiguration.selectAdapters</code> decides which adapters a generator gets: the whole enabled set if the generator configures no <code>adapters</code> list, otherwise only the named ones (unknown names are logged and skipped). Either way the result is sorted with <code>AdapterDependencyComparator</code>.</p><p>That comparator uses the <code>@AdapterDependency({SomeAdapter.class, …})</code> annotations:</p><ol><li>If one adapter transitively declares a dependency on the other, it sorts after it.</li><li>Otherwise the adapters are ordered by declared dependency <strong>depth</strong> — the longest chain of <code>@AdapterDependency</code> hops from the class. An adapter with no annotation has depth 0 and therefore sorts to the front.</li><li>Ties are broken by fully-qualified class name, so the order is deterministic.</li></ol><p>This is a comparator, not a topological sort. It works because the declared dependency graph is shallow and consistent, and because the two loaders (<code>OntologyLoadAdapter</code>, <code>ConceptSchemeLoadAdapter</code>) declare nothing and land at depth 0. A new adapter that forgets its <code>@AdapterDependency</code> inherits that same depth 0 and will run <strong>before</strong> the ontology is loaded — its <code>info.getModel()</code> will be <code>null</code>. See <a href="./extending">Extending</a>.</p><p>The declared dependency graph today:</p>',25)),(r(),s(i,null,{default:t(()=>[c(d,{id:"mermaid-450",class:"mermaid",graph:"flowchart%20TD%0A%20%20OL%5B%22ontology-load%22%5D%20--%3E%20OEE%5B%22ontology-extract-external%22%5D%0A%20%20OEE%20--%3E%20OR%5B%22ontology-reasoner%22%5D%0A%20%20OR%20--%3E%20OCE%5B%22ontology-class-extract%22%5D%0A%20%20OCE%20--%3E%20OUT%5B%22ontology-uri-template%22%5D%0A%20%20OCE%20--%3E%20OPE%5B%22ontology-property-extract%22%5D%0A%20%20OUT%20--%3E%20OPE%0A%20%20OCE%20--%3E%20OPX%5B%22ontology-property-extra%22%5D%0A%20%20OCE%20--%3E%20OIE%5B%22ontology-individuals-extract%22%5D%0A%20%20OPE%20--%3E%20OPO%5B%22ontology-property-override%22%5D%0A%20%20OPE%20--%3E%20ODO%5B%22ontology-datatype-override%22%5D%0A%20%20CSL%5B%22concept-scheme-load%22%5D%20--%3E%20CSE%5B%22concept-scheme-extract%22%5D%0A%20%20CSL%20--%3E%20CCE%5B%22concept-class-extract%22%5D%0A%20%20CSE%20--%3E%20CCE%0A%20%20OCE%20--%3E%20CCE%0A"})]),fallback:t(()=>[...e[1]||(e[1]=[o(" Loading... ",-1)])]),_:1})),e[5]||(e[5]=a("p",null,[o("Arrows point from a dependency to the adapter that declares it. See "),a("a",{href:"./adapters"},"Adapters"),o(" for what each stage actually does and its settings.")],-1)),e[6]||(e[6]=a("h2",{id:"generator-hierarchy",tabindex:"-1"},[o("Generator hierarchy "),a("a",{class:"header-anchor",href:"#generator-hierarchy","aria-label":'Permalink to "Generator hierarchy"'},"​")],-1)),e[7]||(e[7]=a("p",null,"Generators are a four-level inheritance chain. Each level adds a model and leaves rendering to the level below.",-1)),(r(),s(i,null,{default:t(()=>[c(d,{id:"mermaid-460",class:"mermaid",graph:"classDiagram%0A%20%20class%20BaseGenerator%20%7B%0A%20%20%20%20%3C%3Cabstract%3E%3E%0A%20%20%20%20%2BgetName()%20String%0A%20%20%20%20%2Bgenerate()%0A%20%20%20%20%2Brun()%0A%20%20%20%20%2BgetOntologyClasses()%20List~ClassInfo~%0A%20%20%20%20%23saveToFile(path%2C%20content)%0A%20%20%7D%0A%20%20class%20ClassGenerator%20%7B%0A%20%20%20%20%2BgetClasses()%20List~Clazz~%0A%20%20%20%20%23extractClasses()%0A%20%20%20%20%23extractRelations()%0A%20%20%20%20%23filterInterfaces()%0A%20%20%7D%0A%20%20class%20DiagramGenerator%20%7B%0A%20%20%20%20%3C%3Cabstract%3E%3E%0A%20%20%20%20%23saveDiagram(content)%0A%20%20%20%20%23emitStyleDefinitions()%0A%20%20%20%20%23renderContent(builder%2C%20type)%0A%20%20%7D%0A%20%20class%20SchemaGenerator%20%7B%0A%20%20%20%20%3C%3Cabstract%3E%3E%0A%20%20%20%20%23extractTableRelations()%0A%20%20%20%20%23resolveJoinTableName(source%2C%20target)%0A%20%20%20%20%23validateSchema()%0A%20%20%7D%0A%0A%20%20BaseGenerator%20%3C%7C--%20ClassGenerator%0A%20%20ClassGenerator%20%3C%7C--%20DiagramGenerator%0A%20%20DiagramGenerator%20%3C%7C--%20SchemaGenerator%0A%0A%20%20ClassGenerator%20%3C%7C--%20TypescriptGenerator%0A%20%20DiagramGenerator%20%3C%7C--%20ClassDiagramGenerator%0A%20%20SchemaGenerator%20%3C%7C--%20SQLGenerator%0A%20%20SchemaGenerator%20%3C%7C--%20JavaGenerator%0A%20%20SchemaGenerator%20%3C%7C--%20ERDiagramGenerator%0A%20%20SchemaGenerator%20%3C%7C--%20ODCSGenerator%0A%20%20BaseGenerator%20%3C%7C--%20ShaclGenerator%0A%20%20BaseGenerator%20%3C%7C--%20BikeshedGenerator%0A%20%20BaseGenerator%20%3C%7C--%20DataFrameGenerator%0A"})]),fallback:t(()=>[...e[2]||(e[2]=[o(" Loading... ",-1)])]),_:1})),e[8]||(e[8]=n('<p>What each layer contributes:</p><p><strong><code>BaseGenerator</code></strong> holds the three things every generator needs: the <code>OntologyInfo</code>, the <code>ConceptSchemeInfo</code> and the ordered adapter list. Its <code>run()</code> is the pipeline execution itself — it iterates the adapters and calls <code>adapt</code> on each. It also provides <code>getName()</code> (which kebab-cases the simple class name after stripping a trailing <code>Generator</code>), <code>saveToFile</code>, the sorted <code>getOntologyClasses()</code> / <code>getAllClasses()</code> accessors, concept lookup helpers such as <code>getClassConceptForClass</code>, and the <code>toSnakeCase</code> / <code>toKebabCase</code> utilities. <code>generate()</code> simply delegates to <code>run()</code>.</p><p><strong><code>ClassGenerator</code></strong> turns the raw ontology model into a language-neutral class model: lists of <code>Clazz</code>, <code>Interface</code> and <code>Enum</code>, each with <code>Attribute</code>s that carry a data type, range classes and a cardinality. Its <code>run()</code> calls <code>super.run()</code> first — so the adapters execute — and then <code>extractClasses</code>, <code>extractInterfaces</code>, <code>extractEnums</code>, <code>extractMetadataClasses</code>, <code>extractRelations</code>, the filters (inverse properties, interfaces, inherited properties, enums), surrogate keys, identifier fallbacks, range resolution and a final ordering pass that makes output deterministic.</p><p><strong><code>DiagramGenerator</code></strong> (abstract) adds <code>DiagramGeneratorProperties</code>: the style map built from <code>generators.diagram-generator.styles</code>, the shared <code>generate(String type)</code> template that renders a Mermaid document, and <code>saveDiagram(...)</code>. <code>saveDiagram</code> writes the diagram to the configured <code>output-file</code>, prints it to stdout when no output file is set, and — unless <code>export-png: false</code> — renders a PNG next to it via <code>MermaidExporter</code>, which drives a headless browser through Playwright. If the Playwright runtime is missing, the PNG step logs a warning and is skipped; the <code>.mmd</code> file is still written.</p><p><strong><code>SchemaGenerator</code></strong> (abstract) adds the relational model on top of the class model: <code>Table</code>, <code>Column</code> and <code>Relation</code>. Its <code>run()</code> extends the chain with <code>updateEnums</code>, <code>extractTables</code>, <code>extractTableRelations</code>, an ordering pass and <code>validateSchema()</code>. This is the layer that owns identifier-column resolution, <code>xsd:</code> to SQL type mapping, snake_case naming, inheritance flattening, identity tables, many-to-many join tables and the join-table naming patterns from <code>generators.schema-generator</code>. Everything a SQL schema, a JPA entity, an ER diagram and an ODCS contract have in common lives here — which is why those four generators cannot disagree about table names or keys.</p><p>The leaf generators are thin by comparison: they walk the model their base class prepared and append strings to a <code>StringBuilder</code>.</p><h2 id="model-objects" tabindex="-1">Model objects <a class="header-anchor" href="#model-objects" aria-label="Permalink to &quot;Model objects&quot;">​</a></h2><p>The adapters and generators pass around two long-lived objects, both created in <code>bootstrap</code>.</p><p><code>AbstractInfo</code> is the common base. It carries <code>uri</code>, <code>name</code>, <code>label</code>, <code>comment</code>, the originating Jena <code>Resource</code> and a <code>Scope</code> (<code>ONTOLOGY</code> or <code>CONCEPTS</code>); <code>name</code>, <code>label</code> and <code>comment</code> are initialised from the resource&#39;s local name, <code>rdfs:label</code> and <code>rdfs:comment</code>. Equality is by URI.</p><p><code>OntologyInfo</code> is the primary model. It owns:</p><ul><li><code>config</code> — the bound <code>OntologyConfiguration</code>, which is how adapters reach <code>ontology.*</code> settings such as <code>override-properties</code> and <code>surrogate-keys</code>;</li><li><code>concepts</code> — a <code>ConceptSchemeInfo</code>, created in the same constructor;</li><li><code>classesByUri</code> — a <code>LinkedHashMap</code> of <code>ClassInfo</code>, so insertion order is stable and duplicate URIs are ignored (<code>getClasses()</code> returns a snapshot list, <code>addClass()</code> is the way to add);</li><li><code>model</code> — the Jena <code>Model</code> populated by <code>ontology-load</code>;</li><li><code>inferredModel</code> — the Jena <code>InfModel</code> populated by <code>ontology-reasoner</code>;</li><li><code>externalOntologies</code> — nested <code>OntologyInfo</code> instances for imports fetched by <code>ontology-extract-external</code>.</li></ul><p><code>ConceptSchemeInfo</code> is the SKOS side: its own <code>config</code>, its own Jena <code>model</code> loaded by <code>concept-scheme-load</code>, plus <code>classConcepts</code> and <code>propertyConcepts</code>. Generators use it through <code>BaseGenerator.getClassConceptForClass</code> and <code>getPropertyConceptForProperty</code> to attach human labels and descriptions from the vocabulary to classes and properties.</p><p>Below those sit <code>ClassInfo</code>, <code>PropertyInfo</code>, <code>ClassConceptInfo</code>, <code>PropertyConceptInfo</code>, <code>Cardinality</code> and <code>UriTemplate</code>. They are mutable POJOs — adapters enrich them in place, which is why the pipeline order matters so much.</p><h2 id="debugging-a-run" tabindex="-1">Debugging a run <a class="header-anchor" href="#debugging-a-run" aria-label="Permalink to &quot;Debugging a run&quot;">​</a></h2><ul><li>Turn up logging with the <code>ODD_LOG_LEVEL</code> environment variable; <code>logback.xml</code> binds it to the <code>be.vlaanderen.omgeving</code> logger. At <code>DEBUG</code> you see each adapter as it runs (<code>Running adapter: …</code> is logged by <code>BaseGenerator.run</code> at <code>INFO</code>).</li><li>Empty or default-looking output usually means the config file was not read. Check for the <code>Configuration file not found</code> or <code>Unsupported configuration file format</code> warning.</li><li>An adapter that appears not to run may have been filtered out by <code>adapters.&lt;name&gt;.enabled: false</code>, or excluded by an explicit <code>adapters</code> list on the generator — in the latter case look for the <code>Requested adapter &#39;…&#39; is not available or is disabled</code> warning.</li><li>Duplicate column names and similar structural problems are caught by <code>SchemaGenerator.validateSchema()</code>, which throws with the offending table named.</li></ul><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./adapters">Adapters</a> — per-adapter behaviour and settings</li><li><a href="./generators">Generators</a> — per-generator output and settings</li><li><a href="./extending">Extending</a> — adding your own adapter or generator</li><li><a href="./configuration">Configuration Reference</a> — the full key reference</li></ul>',17))])}const E=l(g,[["render",u]]);export{k as __pageData,E as default};
